技术英语批量识图与删除任务

本次内容：
1. 单次最多保留 20 张截图，前端按每组 5 张并发提交，识别结果按分组完整展示，避免 10 张截图只显示首组。
2. 识图历史页新增删除任务按钮；删除当前用户的整次识图会话及未入库来源截图，已入库语料使用的来源对象保留。
3. 删除接口校验当前用户归属，并拒绝删除仍在处理中的会话。

验证：
1. 后端使用 Java 17 执行 `mvn test`：54 个测试通过，1 个对象存储集成测试跳过。
2. 前端执行 `npm ci && npm run build`：通过。
3. 本次部署按 `docs/部署说明.md` 的显式生产主机流程执行，并完成服务健康、接口和 HTTPS 检查。

部署时间、线上提交和备份目录以部署脚本最终输出为准。
本次部署未记录服务器账号、密码、Token 或环境变量真实值。
