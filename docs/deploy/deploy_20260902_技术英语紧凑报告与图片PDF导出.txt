技术英语紧凑识图报告与 PDF/图片导出

本次内容：
1. HTML 导出以用户填写的识图批次名称作为页面标题；历史记录没有批次名称时回退到来源名称。
2. 所有成功识别语料跨分组统一排序，顺序为单词、词组、句子类；失败分组单独提示，不遮挡其他语料。
3. HTML 使用紧凑两栏卡片布局，窄屏自动切换为单栏，并保留释义、音标、句式、重点词和例句。
4. 识图会话和单个批次均新增 PDF 与 PNG 图片导出；PDF 和图片由同一份紧凑 HTML 生成，图片包含全部 PDF 页面。

验证：
1. Java 17 执行 `mvn test`：56 个测试通过，1 个对象存储集成测试跳过。
2. 前端执行 `npm run build`：通过。
3. PDF 导出测试返回 `%PDF`，PNG 导出测试返回有效 PNG 文件头。
4. 部署按 `docs/部署说明.md` 的显式生产主机流程执行；健康接口、核心接口、HTTPS 首页、服务重启检查均通过。

部署时间：2026-09-02 23:13（Asia/Shanghai）
线上提交：`ac74a09`
备份目录：`/opt/ai-techskill-book/backups/20260902_231318`
本次部署未记录服务器账号、密码、Token、API Key 或环境变量真实值。
