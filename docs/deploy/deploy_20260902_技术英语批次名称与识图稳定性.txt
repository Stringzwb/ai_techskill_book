技术英语批次名称与识图稳定性

本次内容：
1. 识图面板新增可手填的识图批次名称，名称保存在识图记录、历史摘要和详情中，不写入技术英语语料来源字段，因此不会出现在语料列表。
2. 单次最多支持 20 张截图，前端按每组 5 张拆分；后端视觉模型请求增加公平并发信号量，默认同一进程一次只执行一个视觉请求，避免多个长请求同时访问上游导致部分分组失败。
3. 请求连接中断后，前端回查服务端识图历史，恢复已经完成、仍在处理或真实失败的分组；处理中分组不会误显示确认入库按钮。
4. 识图历史保留删除任务能力，删除时校验当前用户归属并清理未入库来源图片。

验证：
1. Java 17 执行 `mvn test`：54 个测试通过，1 个对象存储集成测试跳过。
2. 前端执行 `npm run build`：通过。
3. 部署按 `docs/部署说明.md` 的显式生产主机流程执行，并完成服务健康、核心接口、Redis、证书续期定时器和 HTTPS 检查。

部署时间：2026-09-02 19:45（Asia/Shanghai）
线上仓库提交：00effbb
备份目录：`/opt/ai-techskill-book/backups/20260902_194454`

线上检查：
- `/actuator/health` 返回 `{"status":"UP","groups":["liveness","readiness"]}`。
- `/api/ping` 返回正常消息；HTTPS 首页返回 200。
- `ai-techskill-book.service`、`nginx.service`、`redis.service`、`certbot-renew-ai-techskill-book.timer` 均为 active。

本次部署未记录服务器账号、密码、Token、API Key 或环境变量真实值。
