技术英语串行识图与 Terra 模型

本次内容：
1. 识图页面按分组顺序逐组提交，每组完成后再提交下一组，避免多组同步长请求在后端排队并触发代理超时；页面显示当前识别组进度。
2. 保留后端视觉请求单槽位保护，避免不同会话并发压垮上游视觉接口。
3. 默认 AI 模型切换为 `gpt-5.6-terra`；生产环境 `AI_MODEL` 同步切换。
4. 识图过程中禁用重新选择，避免清理正在使用的图片；失败批次仍可从历史记录恢复并重试。

本地模型验证：
- 使用本机受控配置向兼容接口发送一张最小 PNG 视觉请求。
- `gpt-5.6-terra` 返回 HTTP 200，响应包含 1 个结果，未输出密钥或响应正文。

验证：
1. Java 17 执行 `mvn test`：54 个测试通过，1 个对象存储集成测试跳过。
2. 前端执行 `npm run build`：通过。
3. 部署按 `docs/部署说明.md` 的显式生产主机流程执行，并完成服务健康、核心接口、Redis、证书续期定时器和 HTTPS 检查。

部署时间：2026-09-02 22:37（Asia/Shanghai）
线上提交：`1a0b4f3`
备份目录：`/opt/ai-techskill-book/backups/20260902_223757`
本次部署未记录服务器账号、密码、Token、API Key 或环境变量真实值。
