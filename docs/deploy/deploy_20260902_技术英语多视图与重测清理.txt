技术英语多视图与重测清理

本次发布内容
1. 技术英语语料列表增加“语料速览”高密度视图；原有紧凑列表和卡片网格继续保留，用户的选择保存在本机浏览器。
2. 句子语料的列表响应增加句式框架和解析字段，使语料库列表可直接显示已识别的可复用框架。
3. 识图记录导出的 HTML 默认采用紧凑排版，并提供紧凑、卡片、阅读三种本地切换视图。
4. 按测试重置授权，发布与线上核验完成后清理技术英语语料、识图任务、关联数据、来源图片和短期识别草稿；知识标签及其他模块数据不在清理范围内。

部署前验证
- 后端：使用 Java 17 执行 mvn clean test package，通过 53 项测试；1 项可选对象存储集成测试跳过。
- 前端：执行 npm run build 通过。
- 生成 HTML 单元测试覆盖三种视图控制与内容转义。
- 本文件不包含服务器地址、账号、密码、Token 或环境变量真实值。
