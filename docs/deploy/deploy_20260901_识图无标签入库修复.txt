技术英语 AI 识图无标签入库与历史批次修复

前端更新内容
1. 知识标签改为可选，未标注结果也可以直接入库
2. 标签面板默认展示完整标签树路径，搜索时显示匹配项
3. 每条语料继续支持独立追加和清空标签

后端更新内容
1. 历史批次确认入库允许不重新上传图片，复用已保存截图
2. AI 识图语料允许不绑定知识标签
3. 增加无标签确认入库回归测试

数据库更新内容
1. 无

中间件更新内容
1. 无
